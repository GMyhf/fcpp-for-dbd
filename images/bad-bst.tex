\documentclass[tikz]{standalone}

\usepackage{ctex}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}   
\usetikzlibrary{positioning, arrows.meta, intersections, calc, fit, backgrounds}
\usepgfplotslibrary{fillbetween}
\begin{document}
\begin{tikzpicture}[
    node distance = 0.8cm, align=center,
    level/.style={sibling distance=20mm/#1},
    every node/.style={draw, font=\ttfamily, sibling distance=1.5cm},
]

\node {6}
    child {node {5}
        child {node {4}
            child {node {3}}
            child[missing]
        }
        child[missing]
    }
    child {node {7}
        child[missing]
        child {node {8}}
    };

\end{tikzpicture}
\end{document}